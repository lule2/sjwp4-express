<!DOCTYPE html>
<html data-theme="synthwave">

<head>
    <title>Moja Express aplikacija</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    {{#if user}}
    <div class="flex flex-wrap gap-2 bg-base-200 p-2">
        <div class="flex-1">
            <a gref="/competitions" class="btn btn-sm btn-accent">Natjecanja</a>
        </div>
        <div class="flex-none flex just-end items-baseline gap-2">
            <a gref="/users/data" class="btn btn-sm btn-secondary">{{user.name}}</a>
            <a gref="/users/signout" class="btn btn-sm btn-accent">Odjava</a>
        </div>
    </div>
    {{/if}}
    {{{body}}}
</body>

</html>